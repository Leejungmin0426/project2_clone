<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.studybridge.like.LikeMapper">

    <!-- 좋아요 등록 -->
    <insert id="insertLike">
        INSERT INTO `like` (user_id, aca_id)
        VALUES (#{userId}, #{acaId})
    </insert>

    <!-- 좋아요 삭제 -->
    <delete id="deleteLike">
        DELETE FROM `like`
        WHERE user_id = #{userId} AND aca_id = #{acaId}
    </delete>

    <!-- 특정 학원의 좋아요를 누른 유저들의 프로필 사진 조회 -->
    <select id="getLikedUserPics">
        SELECT DISTINCT u.user_pic
        FROM `like` l
        JOIN user u ON l.user_id = u.user_id
        WHERE l.aca_id = #{acaId}
    </select>

    <!-- 특정 유저가 좋아요한 학원 목록 조회 -->
    <select id="getUserLikedAcademies">
        SELECT aca_id
        FROM `like`
        WHERE user_id = #{userId}
    </select>


</mapper>